# environment.yml
# Grouped + updated for Spotify Churn Analysis (STAT 159/259)
name: final-group08

channels:
  - conda-forge
  - nodefaults
  # NOTE: keep defaults only if you truly need it; conda-forge is usually enough
  - defaults

dependencies:
  # ---------------------------------------------------------------------------
  # 00. Core / Package Management 
  # ---------------------------------------------------------------------------
  - python=3.12
  - pip
  # - pixi
  # - uv

  # ---------------------------------------------------------------------------
  # 01. Jupyter / Authoring / Export
  # ---------------------------------------------------------------------------
  - jupyterlab
  - jupyterlab-myst
  - ipykernel
  - ipywidgets
  - jupyter-ai
  - jupyter-ai-magics
  - jupytext
  - mystmd
  - nbconvert
  - pandoc
  - texlive-core
  # - texlive-fonts-recommended
  # - texlive-latex-recommended
  # - texlive-latex-extra
  # - texlive-bibtex-extra
  - typst
  - typst-py

  # ---------------------------------------------------------------------------
  # 02. Data I/O / Reproducibility / Validation
  # ---------------------------------------------------------------------------
  - numpy
  - pandas
  - scipy
  - pyyaml
  - python-dotenv
  - tqdm
  - joblib
  - pandera
  - pyjanitor
  - mlcroissant
  - xarray

  # ---------------------------------------------------------------------------
  # 03. EDA / Visualization
  # ---------------------------------------------------------------------------
  - matplotlib
  - seaborn
  - missingno
  - plotly

  # ---------------------------------------------------------------------------
  # 04. Modeling (Core)
  # ---------------------------------------------------------------------------
  - scikit-learn
  - imbalanced-learn
  - category_encoders
  - xgboost
  - lightgbm
  # - tabnet
  # - pytorch-tabnet

  # ---------------------------------------------------------------------------
  # 05. Explainability / Diagnostics
  # ---------------------------------------------------------------------------
  - shap
  - yellowbrick

  # ---------------------------------------------------------------------------
  # 06. Testing / Dev Tools
  # ---------------------------------------------------------------------------
  - hypothesis
  - pytest
  - black
  - isort
  - flake8
  - nbqa

  # ---------------------------------------------------------------------------
  # 07. Web / Automation (if you truly need browser automation)
  # ---------------------------------------------------------------------------
  - playwright

  # ---------------------------------------------------------------------------
  # 08. pip extras (project install)
  # ---------------------------------------------------------------------------
  - pip:
      - -e .
      # - tabnet
      # - pytorch-tabnet

###
# Removed prefix: (better for sharing + Binder/CI friendliness)
###

# prefix: /home/jovyan/.local/share/envs/final-group08